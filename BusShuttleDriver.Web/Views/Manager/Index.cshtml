@using BusShuttleDriver.Web.ViewModels
@model IEnumerable<BusShuttleDriver.Web.ViewModels.DriverViewModel>

<h2>Drivers</h2>
<a asp-controller="Driver" asp-action="Create" class="btn btn-primary">Create Driver</a>
<table class="table">
    <thead>
        <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email</th>
            <th>Role</th>
            <th>Status</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var driver in Model)
        {
            <tr>
                <td>@driver.Firstname</td>
                <td>@driver.Lastname</td>
                <td>@driver.Email</td>
                <td>@driver.Role</td>
                <td>@(driver.IsActive ? "Active" : "Inactive")</td>
                <td>
                    @if (!driver.IsActive)
                    {
                        <form asp-action="ToggleActiveStatus" asp-controller="Manager" asp-route-id="@driver.Id" method="post">
                            <input type="submit" value="Activate" class="btn btn-primary" />
                        </form>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
<div class="col-md-6 text-right">
    <a href="@Url.Action("Dashboard", "Manager")" class="btn btn-primary mt-2">Back to Dashboard</a>
</div>
